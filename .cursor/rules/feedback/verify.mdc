---
description: 验收命令与分层 — gate-check · task-verify · verify 脚本；编辑 plan VERIFY 或 scripts/ 时加载
globs:
  - .cursorGrowth/plan.md
  - plan.md
  - scripts/verify.sh
  - scripts/test.sh
  - .cursor/config/workflow.json
alwaysApply: false
---

# 验收

遵循 `rules/workflow.mdc`：`gate-check`/`task-verify` 非 OK 不得 ✅ 或 commit；打 tag 前 `verify`。

## 命令

```bash
./.cursor/bin/runner.sh gate-check
./.cursor/bin/runner.sh task-verify    # 当前 ACTIVE 任务
./.cursor/bin/runner.sh verify         # Sprint / 打版前全量（读 plan VERIFY 后执行脚本）
```

**勿混淆**：`runner.sh verify` 是 **CLI 入口**；plan 工作副本（`workflow.json` → `plan_file`）里的 `<!-- VERIFY: ... -->` 必须指向 **项目验收脚本**（默认 `./scripts/verify.sh`），**禁止**写 `runner.sh verify`（会无限递归）。

## 验收列约定

| 阶段 | 推荐 |
|------|------|
| 每个 TASK · `task-verify` | `./scripts/verify.sh`（**L1**，~20s） |
| merge / 高频 patch 前 | `npm run verify:core`（**L2-core**，~2–4min） |
| Sprint 收尾 · 打 tag 前 | `npm run verify:standard`（**L2**，优先 CI） |
| CI / nightly | `./scripts/verify.sh --full`（**L3**） |

**禁止**把 `--standard` 或 `--full` 当每个 TASK 的默认门禁。plan `<!-- VERIFY: ... -->` 写 Sprint 出口层（通常 `verify:core` 或 `verify:standard`），TASK 验收列写 L1 或域脚本。

| 阶段（简表） | 推荐 |
|------|------|
| 开发任务 | `./scripts/test.sh`（= L1 子集） |
| 任务收尾 / P0 | `./scripts/verify.sh` |

`task_verify_heuristics.enabled` 默认 **false**（`config/workflow.json`）。设为 `true` 时，描述性验收列回退 `fallback_test_script` / `fallback_verify_script`。

用户可见修复 → CHANGELOG `### Fixed`。

## 分层验收（verify-layers）

大型或跨模块功能：除 `./scripts/test.sh` / `./scripts/verify.sh` 外，项目可增设 **按域脚本**（命名示例 `scripts/verify_<feature>.sh`，路径由项目自定）。

| 层 | 典型内容 | 外部依赖 | 默认 CI |
|----|----------|----------|---------|
| **L0** | 关键文件 / 配置存在 | 无 | ✅ |
| **L1** | import · 单元测试 · 静态检查 | 无 | ✅ |
| **L2-core** | P0 切片并行（例：`verify-core.sh`） | 无 · 无 Docker 默认可 SKIP | merge 前 |
| **L2** | 全量切片（例：`--standard`） | 部分 slice 需 Playwright/Docker | Sprint / 打版 |
| **L3** | `--full`：L2 + HEAVY + build + perf | 基础设施 | CI / nightly |

约定：

- **默认**（无参）：跑到 **L1**（或项目文档约定的最快绿层）
- **`--full`**：跑到 **L3**
- **聚合脚本**（示例 `scripts/verify_all.sh`）：顺序调用各域脚本；新域交付时 **注册一行**，勿复制整套逻辑进每个域脚本
- **目录落点**：域脚本 `verify/domain/` · tier orchestrator `verify/tier/` · 公因子 `lib/_*.sh` · 工具 `ops/`/`dev/` → **plan** `reference/growth-layout.md`（Growth `archive/{domain}/` 同源）

plan **验收列**写可执行命令（如 `bash scripts/verify_<feature>.sh`）；Sprint 全量走 `plan VERIFY` 或 `./scripts/verify.sh`。

## Super Cursor layout 验收（`verify-super-cursor.sh`）

与 **task-verify / `scripts/verify.sh`** 不同：校验 `.cursor/` 母版 **文件布局与自洽**（非业务测试）。

| 模式 | 判定 | 纯母版项（`install-super-cursor.sh` · 禁止根 `scripts/` · `.github/workflows/verify.yml`） |
|------|------|----------------------------------------------------------------------------------------------|
| **mother** | 纯母版空仓；或 `SC_VERIFY_LAYOUT=mother` | **必须**满足 |
| **hybrid** | 根目录有 `scripts/` · `backend/` · `frontend/` 等业务树（自动）；或 `SC_VERIFY_LAYOUT=hybrid` | **SKIP**；改验 `scripts/verify.sh` 存在 |

混合仓日常：**`cursor-coherence.sh`** + **`verify-super-cursor.sh`**；纯母版空仓：**`template-verify.sh`** 全量。详见 `.cursor/README.md` §自测。

**L0–L3 真源在本文件**；**test** skill 只引用本表，勿另写一份层定义。测试文件细则 → `rules/execution/testing.mdc` · 垂直切片层序 → `rules/execution/vibe.mdc`。

## task-verify 语义（fail-closed）

`./.cursor/bin/runner.sh task-verify` 按 `acceptance_kind()` 判定，**描述性验收不再静默通过**：

| 验收列形态 | 行为 |
|------------|------|
| 可执行命令（`./…` · `npm …` · `pytest …` · `bash …`） | 执行，按其退出码 |
| `manual: <步骤与证据>` | **唯一豁免** → 0；证据须落 plan/CHANGELOG |
| 描述性文字 + `fallback_test_script` 存在（heuristics 开） | 跑兜底脚本 |
| 描述性文字（其余） | **FAIL + 退出 1**，并给出改写建议 |

`plan-check` 会在**开工前**对 prose 验收列 WARN 并计入 issues。写法 → `.cursor/templates/plan.md`。

## 母版门禁清单（11 道 · 全部接线）

`verify-super-cursor.sh` 聚合以下子验证器；`template-verify.sh` 是母版完整入口：

| 门禁 | 守护什么 | 触发 |
|------|----------|------|
| `verify-rules-globs.sh` | rule frontmatter/键 · 禁 brace glob · plan 闸门 glob 与 7 栈真实文件命中 | 通用 |
| `verify-portability.sh` | GNU-only 构造 · 非 POSIX `\s` · 硬编码 `python3` · shebang · **技能平台作用域 ⇔ 文档** | 通用 |
| `verify-config.sh` | `config/schema.json` 校验 6 个 config：未知键/类型错误 **即 FAIL**（不再静默回退） | 通用 |
| `verify-roo-compat.sh` | frontmatter 键 ⊆ 开放协议白名单（编辑器私有键即 FAIL）· rules 用 `.mdc` | 通用 |
| `verify-secrets.sh` | 已跟踪文件里的密钥/凭据/被跟踪 `.env` | 通用 |
| `verify-doc-super-cursor.sh` | 门面计数 · doc 相对链接 · skill `description ≤85` · dmi 分桶 · run/plan SKILL 体积 · 双语入口 | 母版 |
| `verify-growth-layout.sh` | `archive/{domain}` 与 `scripts/` 分级引用 | 母版 |
| `consumer-smoke.sh` | **目标项目端到端**：install → 闸门 BLOCK → hook → 批准放行 → 目标仓库全绿 | 母版 |
| `verify-changelog.sh` | CHANGELOG 结构：`[Unreleased]` 存在 · **同版本不得重复小节** · 落地区小节顺序 Added→Changed→Fixed（已发布节点不重排） | 通用 |
| `verify-docs-layout.sh` | 项目文档体系：号位=瀑布阶段 · 必选号位 · ≤10 · 号唯一 · `ROADMAP.md` 例外 · 生成标记；无编号文档时渐进跳过并校验模板 | 通用 |
| `verify-scripts-layout.sh` | `scripts/` 分层：根只留入口 · `scripts/README.md` · 根脚本体量 · **重复块检测**（≥4 行相同 → 提取到 `lib/_*.sh`）；无 `scripts/` 时改查 scaffold 模板 | 通用 |

**接线自检**：`bin/verify-*.sh` 必须被 `verify-super-cursor.sh` 或 `template-verify.sh` 调用，否则 FAIL —— 防止"脚本存在但从未生效"（本项目曾有两个验证器长期未接线）。

**新增一门禁的固定动作**：脚本 + 至少一个负向测试 + 接入聚合 + 更新本节清单 + 需要时同步 `.cursor/README.md` / 根 `README.md` §验证。

## 跨平台与 CI

- 无 `jq` 时走 python 回退；`SC_FORCE_PYTHON=1` 可**强制**该路径（CI 用它覆盖非 jq 分支）
- CI 矩阵：ubuntu（+jq，含文档构建）· macos（BSD 工具链）· windows/Git Bash（非阻塞观察中）
- 平台作用域与技能限制 → `docs/platforms.md` §技能平台作用域
