# 开发规范

> 号位 `06` · 阶段：工程规范（必选）

## 1. 环境与启动

```bash
# 安装依赖 / 启动 / 运行
```

## 2. 目录与命名

- 分层：
- 命名：

## 3. 分支与提交

- 分支策略：
- 提交信息：Conventional Commits（见 `rules/execution/commit.mdc`）

## 4. 测试策略

- 框架（见 `rules/execution/testing.mdc` §框架选型）：
- `scripts/test.sh`（开发循环）· `scripts/verify.sh`（全量）：
- 覆盖要求：

## 5. 质量门禁

| 阶段 | 命令 |
|------|------|
| 每次改动 | `./scripts/test.sh` |
| 任务收尾 | `./scripts/verify.sh` |
