<!-- generated: manual · regenerate: /manual -->
# 使用文档（占位 — 由 /manual 生成）

> ⚠️ **不要手写本文件**。号位 `07` 由 **`/manual`**（user-manual skill · Manual Contract）生成。

步骤：

1. 编辑 `config/manual.yaml`（`doc_path` 指向本项目 `docs/07_<名称>.md`，`assets_dir` 放配图）
2. 运行 `/manual`（或按 `skills/user-manual/reference/pipeline.md` 五段流水线）
3. 生成后勿手改；要改先改契约再重新生成

契约示例 → `.cursor/templates/manual-contract.example.yaml`
