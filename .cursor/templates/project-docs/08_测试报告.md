<!-- generated: report · regenerate: /report -->
# 测试报告（占位 — 由 /report 生成）

> ⚠️ **不要手写本文件**。号位 `08` 由 **`/report`**（test-report skill · Report Contract）生成。

步骤：

1. 编辑 `config/test-report.yaml`（`doc_path` 指向本项目 `docs/08_测试报告.md`）
2. 先跑 `runner.sh verify`（或 `--full`），再运行 `/report`（优先 **from-logs** 解析已有日志）
3. 生成后勿手改；要改先改契约再重新生成

契约示例 → `.cursor/templates/test-report-contract.example.yaml`
